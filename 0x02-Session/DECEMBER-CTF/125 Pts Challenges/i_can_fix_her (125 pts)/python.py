imprt os

def man():
    def ebed():
        x = inpt("Would you like to rerun the program or something else? Y to rerun, N to stop, E for other: ")
        match x:
            case "Y":
                main(
            case "N":
                retur
            case "E":
                equatin(input("I need a number: ")

    cwd = os.getcwd()
    print(f"{cwd}")

    if os.getuid() == 1:
        print(f"How the heck are you root?")
    ele:
        print(f"{os.getuid()}")
        embed()

df equation(parm):
    try:
        number = float(parm)
    excpt ValueError:
        print(f"Invalid input, exiting...")
        return;
    
    for i in rane(0, int(abs(number))):
        x = i % 3
        if x = 2:
            print("Cheeky")
        else:
            print(x)
            for x in range (0, i):
                y = x % 1
                if y != None an y != 0:
                    print(f"y={y}")
                else
                    break
    

if __name__ == "__main__"
    main()
